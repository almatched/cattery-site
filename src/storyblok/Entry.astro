---
import { storyblokEditable } from "@storyblok/astro";
import type { EntryStoryblok } from "component-types-sb";

interface Props {
    language: string;
    blok: EntryStoryblok;
}

const { blok } = Astro.props;
---

<Fragment {...storyblokEditable(blok)}>{blok.value}</Fragment>
