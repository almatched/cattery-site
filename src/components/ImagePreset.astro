---
import { Picture } from "astro:assets";

const { src, alt, width, height } = Astro.props;
---

<Picture
  src={src + `/m/${width}x${height}`}
  alt={alt ?? "image without description"}
  width={width}
  height={height}
  class="object-cover object-center w-full h-full"
/>
